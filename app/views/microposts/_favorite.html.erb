<%= form_for(current_user.favorites.build, remote: true) do |f| %>
  <div><%= hidden_field_tag :favorite_id, favorite.id %></div>
  <%= f.submit t('common.favorite'), class: "btn btn-primary" %>
<% end %>