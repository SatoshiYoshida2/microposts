<%= form_for(current_user.favorites.find_by(favorite_id: favorite.id),
             html: { method: :delete },
             remote: true) do |f| %>
  <div><%= hidden_field_tag :favorite_id, favorite.id %></div>
  <%= f.submit "Unfavorite", class: "btn" %>
<% end %>