<header class="navbar navbar-fixed-top navbar-inverse">
  <div class="container">
    <%= link_to "Microposts", root_path, id: "logo" %>
    <nav>
      <ul class="nav navbar-nav navbar-right">
        <li><%= link_to t('common.home'), root_path %></li>
        <li><%= link_to t('common.help'), '#' %></li>
        <% if logged_in? %>
        
          <!-- <li><%#= link_to t('common.users'), '#' %></li> -->
          
          <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown">
              <%= t('common.users') %> <b class="caret"></b>
            </a>
            <ul class="dropdown-menu">
              <li><%= link_to t('common.followings'), followers_user_path(current_user) %></li>
              <li><%= link_to t('common.followers'), followings_user_path(current_user) %></li>
            </ul>
          </li>
          
          <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown">
              <%= t('common.account') %>  <b class="caret"></b>
            </a>
            <ul class="dropdown-menu">
              <li><%= link_to t('common.profile'), current_user %></li>
              <li><%= link_to t('common.favorites'), favorites_user_path(current_user) %></li>
              <li><%= link_to t('common.settings'), edit_user_path(current_user) %></li>
              <li class="divider"></li>
              <li>
                <%= link_to t('common.logout'), logout_path, method: "delete" %>
              </li>
            </ul>
          </li>
        <% else %>
          <li><%= link_to t('common.login'), login_path %></li>
        <% end %>
      </ul>
    </nav>
  </div>
</header>